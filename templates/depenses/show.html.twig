{% extends 'base.html.twig' %}

{% block title %}Depenses{% endblock %}

{% block body %}
    <h1>Depenses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ depense.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ depense.montant }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ depense.createdAt ? depense.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('depenses_index') }}">back to list</a>

    <a href="{{ path('depenses_edit', {'id': depense.id}) }}">edit</a>

    {{ include('depenses/_delete_form.html.twig') }}
{% endblock %}
