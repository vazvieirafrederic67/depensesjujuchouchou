{% extends 'base.html.twig' %}

{% block title %}Depenses index{% endblock %}

{% block body %}
    <div style="height:12vh;background-color: #F0F8FF;" class="d-flex justify-content-between align-items-center">
        <h1>Depenses</h1><h2>{{ date|date('d-m-y') }}</h2>
    </div>
    
    <div class="d-flex flex-wrap justify-content-start">
    
        {% for depense in depenses %}

            <div class="card col-sm-12 col-md-6 col-lg-3" style="height:auto;">
                <div class="card-body">
                    <h3 class="card-title">{{ depense.category.category }} </h3>
                    <ul>
                        <li><h5>Montant : {{ depense.montant }} € </h5></li>
                        <li><h6>Date : {{ depense.createdAt ? depense.createdAt|date('Y-m-d') : '' }}</h6></li>
                    </ul>
                </div>
            </div>
        {% else %}
                <td colspan="4">no records found</td>
        {% endfor %}
    </div>

  
        


    
{% endblock %}